# Requirements for DINOv2 Feature Extraction
# Install with: pip install -r requirements_dinov2.txt

# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.21.0
pandas>=1.3.0
Pillow>=9.0.0
tqdm>=4.62.0

# Optional but recommended for downstream analysis
scikit-learn>=1.0.0  # For clustering, similarity, etc.
umap-learn>=0.5.0    # For dimensionality reduction
matplotlib>=3.5.0    # For visualization

# Note: DINOv2 model is downloaded automatically via torch.hub
# No need to clone the repository or install separately
